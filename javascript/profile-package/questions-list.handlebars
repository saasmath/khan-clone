<div style="font-size: 14px;">
{{#if this.length}}
    {{#each this}}
    <div style="margin-left: -10px;">

        <ul>
        <li>
            <div class="question clearfix {{#if hasUnread}}unread{{/if}} visited-no-recolor">
                <div style="white-space: nowrap; overflow: hidden;text-overflow: ellipsis;">
                <a class="discussion-link" href="/{{topicSlug}}/v/{{video.readableId}}?qa_expand_key={{qaExpandKey}}">{{pluralize answererCount "person"}} answered your question</a>
                <span>{{content}}</span>
                </div>
                on "<a href="/{{topicSlug}}/v/{{video.readableId}}" style="color: #777">{{video.title}}</a>"
                <div class="timeago" title="{{lastDate}}">{{lastDate}}</div>
            </div>
        </li>
        </ul>
    </div>
    {{/each}}
{{else}}
    You haven't asked any questions. Go <a href="/#browse">watch a video</a> and ask a question!
{{/if}}
</div>
