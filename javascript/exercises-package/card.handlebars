<div id="card-cid-{{cid}}" class="card-container">
    <div class="card">
        {{#if frontVisible}}
        <div class="card-front card-face">
            
            <span>
                <!--{{index}} ({{leavesEarned}}) ({{cardType}})-->
                {{> card-leaves}}
            </span>
            
        </div>
        {{else}}
        <div class="card-back card-face">
            &nbsp;
        </div>
        {{/if}}
    </div>
</div>
