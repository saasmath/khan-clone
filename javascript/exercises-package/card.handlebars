<div class="card-container">
    <div class="card">
        <div class="card-front card-face">
            {{index}} - {{leavesEarned}}

            {{#if frontVisible}}
                (front)
            {{else}}
                (back)
            {{/if}}
        </div>
        <div class="card-back card-face">
            &nbsp;
        </div>
    </div>
</div>
